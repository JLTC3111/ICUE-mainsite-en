.DS_Store
**/.DS_Store
netlify/functions/private-env.cjs
# Dependencies are installed by the build (npm ci) — never commit them.
node_modules/
*.local

# Vite / postbuild output. Netlify runs `npm run build` and publishes dist-home.
# `dist` does not match dist-home or sibling-app outDirs (newsroom, people, …).
dist
dist-ssr
dist-home/

# Mirrors generated by home-app/scripts/sync-assets.mjs from canonical /public.
/home-app/public/certs/
/home-app/public/files/
/home-app/public/music/

# Home-app hashed bundles and generated HTML copied to repo root by local dev.
/assets/
/route-shells/
/index.html

# Sibling-app Vite outDirs. Source lives in *-app/; public assets stay in public/.
/newsroom/
/people/
/structure/
/our-work/
/contact/
/legal/
/faqs/
/recruitment/

# Environment (secrets — never commit)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

!public/fonts/.gitkeep
